diff --git a/CMakeLists.txt b/CMakeLists.txt
index a8129f0..a2dd655 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -5,6 +5,11 @@ if(CMAKE_COMPILER_IS_GNUCXX OR CMAKE_CXX_COMPILER_ID MATCHES "Clang")
   add_compile_options(-Wall -Wextra -Wpedantic)
 endif()
 
+# this package depends on pcl_ros, which depends on VTK, which depends on MPI
+# but since nvidia have a custom MPI installation, we need to explicitly find it
+# here to avoid CMake picking up the wrong MPI installation when finding VTK
+find_package(MPI)
+
 find_package(ament_cmake_auto REQUIRED)
 ament_auto_find_build_dependencies()
 
